echo $PATH
# Install puppetlabs upstream repo
#curl -o /tmp/puppetlabs-release-wheezy.deb https://apt.puppetlabs.com/puppetlabs-release-trusty.deb
#dpkg -i /tmp/puppetlabs-release-wheezy.deb
#apt-get update -y

# Install puppet
#apt-get install puppet -y
